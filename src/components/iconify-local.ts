import { addCollection } from "@iconify/react";
import materialSymbols from "@iconify-json/material-symbols/icons.json";

addCollection(materialSymbols as any);

export function ensureLocalIconifyCollections() {}
